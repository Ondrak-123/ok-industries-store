{
  "name": "ok-industries-server",
  "version": "1.0.0",
  "description": "JSON Server for OK Industries",
  "main": "server.js",
  "scripts": {
    "start": "npm install && node server.js",
    "dev": "json-server --watch db.json --port 3001",
    "build": "npm install"
  },
  "dependencies": {
    "json-server": "^0.17.4",
    "cors": "^2.8.5"
  },
  "engines": {
    "node": "18.x"
  }
}